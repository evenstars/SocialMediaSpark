# ---- Core ----
langgraph>=0.2          # orchestration (graph.py has a fallback executor when it can't be installed)
numpy>=1.26
pillow>=10.0

# ---- Tests ----
pytest>=8.0

# ---- M5 real models (API-first, install when needed) ----
# openai>=1.0           # hosted image API
# insightface           # local face vectors (for guardrails), or use a cloud face-compare API

# ---- M6 review UI ----
# gradio>=4.0

# ---- v2 / cloud (when needed) ----
# psycopg[binary]>=3.1  # PostgreSQL
# pgvector              # vector search
# boto3                 # object storage
